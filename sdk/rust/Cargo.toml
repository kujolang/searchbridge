[package]
name = "searchbridge"
version = "1.0.0"
edition = "2021"
license = "MIT"
description = "Typed local CLI SDK for SearchBridge"
repository = "https://github.com/kujolang/searchbridge"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
